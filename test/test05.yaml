# This is to test schedule up-propagation
tree:
  id: root
  title: test01
  max_proc: 2
  envs:
    TERM: xterm-256color
    COLORTERM: truecolor
  vars:
    thingy: "thing!"
  include:
    - "external"
    - "external"

external:
  type: "par"
  title: "Test incluye {thingy}"
  include:
    - "a1"
    - "a2"

a1:
  args: ["/bin/bash"]

a2:
  args: ["/bin/bash"]
